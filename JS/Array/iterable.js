
let colors = ["red", "yellow", "green", "blue", "nude"];
//              0       1         2        3
// colors is iterable object and it's nothing but array

for( let color of colors) {
    console.log(color)
}